# ==========================================
# ROBO Data Catalog 환경변수 설정
# ==========================================
# 이 파일을 .env로 복사하여 값을 설정하세요.
# cp .env.example .env
# ==========================================

# ------------------------------------------
# 필수 설정
# ------------------------------------------

# Neo4j 데이터베이스 연결
NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_password
NEO4J_DATABASE=neo4j

# LLM API 키 (description 생성용)
LLM_API_KEY=sk-your-api-key-here

# ------------------------------------------
# LLM 설정 (선택)
# ------------------------------------------
# LLM_MODEL=gpt-4.1
# EMBEDDING_MODEL=text-embedding-3-small

# ------------------------------------------
# 서버 설정 (선택)
# ------------------------------------------
# HOST=0.0.0.0
# PORT=5503

# ------------------------------------------
# Text2SQL 연동 (샘플 데이터 조회)
# ------------------------------------------
TEXT2SQL_API_URL=http://localhost:8000

# ------------------------------------------
# 메타데이터 보강 설정 (선택)
# ------------------------------------------
# FK 관계 추론
# FK_INFERENCE_ENABLED=true
# FK_SAMPLE_SIZE=25
# FK_SIMILARITY_THRESHOLD=0.8
# FK_MATCH_RATIO_THRESHOLD=0.8
# FK_CONCURRENCY=5

# 타임아웃 (초)
# METADATA_TIMEOUT_CONNECT=5
# METADATA_TIMEOUT_REQUEST=30
